# Бэклог Vokal Director

Обновлен: 2026-09-09. Груминг через `vokal-product-agent` в шесть параллельных пачек,
порядок — WSJF. Сведение, проверка фактов и правки — на ведущем агенте.

`CoD = ценность + срочность + снижение риска`, `WSJF = CoD / размер`.
Шкала по каждой оси: 1 / 2 / 3 / 5 / 8 / 13 / 20. Размер — относительная
трудоемкость, не часы. Сроков в днях и неделях в этом документе нет намеренно.

**WSJF не равен порядку работ.** Три систематические поправки:
дизайн-док интеграции по `CLAUDE.md` пишется прямо перед сборкой, а не по приоритету;
измерять надо раньше, чем ограничивать; самый заблокированный батч не ставится
раньше самого дешевого полезного.

## Сделано

| # | Батч | Итог |
| --- | --- | --- |
| B1 | Убрать обещание срока обработки, починить сторож честности | `706c6c4` |
| — | Сторож не мог упасть на кириллице: `\b` в JS работает только по ASCII | `fb29eaf` |
| B55 | Чек-лист go-live и брифинг юристу | `docs/GO_LIVE.md`, `docs/LEGAL_BRIEF.md` |
| B56 | Правовая позиция по чужим фонограммам | `docs/LEGAL_POSITION.md` |
| B57 | Карта данных, 152-ФЗ, локализация, оператор | `docs/DATA_MAP.md` |
| B60 | Спека гарантий удаления и сроков хранения | `docs/DELETION_AND_RETENTION_DESIGN.md` |
| B3a | Гайд валидации Phase 0 | `docs/VALIDATION_PHASE0.md`, `docs/VALIDATION_LOG.md` |
| B2 | Стенд: относительный base, workflow, noindex, README | сама выкладка за владельцем |
| B4a | Правда о файле: настоящие факты вместо чужого разбора | спека + 22 новых теста |
| — | Фундамент дизайн-системы: токены, шрифты, кнопки, пороги | `ee4e84e` |
| B70 | Регрессии B4a и пустые состояния пути загрузки | по грумингу UX и функциональности |

Ожидают внешней стороны: **B11b** (юрист), **B3b** (интервью владельца),
**B12b** (ключи и бюджет). Ответы юриста разблокируют пункты 1-4 чек-листа
go-live и позволяют дописать сроки в спеке удаления.

## Развилки владельца

Порядок принятия важнее скорости: сначала то, где срок ответа не наш.

**Немедленно, параллельно всему — запросы наружу.** Письмо юристу и запрос прайса
и тестовых ключей у провайдеров. Ответы идут неделями, и день ожидания бесплатен
только если ожидание уже началось.

| # | Развилка | Рекомендация | Обратимость |
| --- | --- | --- | --- |
| 1 | Backend-стек: NestJS или FastAPI | **FastAPI**: воркеры все равно Python, иначе два рантайма при команде в одного человека. Переворачивается, если нанимаете TS-разработчика | фиксирует надолго |
| 2 | Stems: AudioShake или Demucs self-host | **Demucs**: чужие фонограммы не уходят третьей стороне, что сильнее для РФ и для обещания приватности | наполовину: измерение обратимо, выбор фиксирует B18 |
| 3 | Где физически лежат файлы (152-ФЗ, кто оператор ПДн) | нужен ответ юриста, не наш | фиксирует надолго |
| 4 | Правовая позиция по чужим фонограммам | экзистенциальная, см. B56 | фиксирует продукт целиком |
| 5 | Сегмент V1: кавер-группы или музыкальная школа | по результатам интервью B3b | дешево передумать |
| 6 | Хостинг стенда: открытый URL или по секрету | **открытый с `noindex`**: трение на интервью дороже теоретического репутационного риска | дешево передумать |
| 7 | Модель монетизации | **гибрид**: группам за песню, школам годовая квота по счету. Подписку не вводить, пока метринг не покажет 3+ песни в месяц у медианной группы | фиксирует ожидания клиентов |

**Блокирует старт Phase 1:** развилка 1 напрямую; развилки 3 и 4 — через первую
интеграцию, дизайн-док хранилища без них не дописать.

**Не блокирует разработку:** развилки 5, 6, 7, юр-тексты, бюджет на бенчмарк,
чистка `.claude/skills`.

Три позиции разбиты, потому что доступная половина простаивала под чужим
блокером: **B11a** (реестр обещаний и модуль констант) делается без юриста, ждет
только **B11b**; **B12a** (протокол, корпус, разметка, прогон Demucs) не требует
ни ключей, ни бюджета — их ждет только **B12b**; **B14a** (проверка и отчет)
детерминирован, а **B14b** нельзя оценить до отчета. Так же разбиты **B3**
(гайд отдельно от проведения интервью) и **B4** (правда о файле отдельно от звука):
первая половина каждого уже дает результат.

## Слой 0: исполнимо прямо сейчас

Без backend, без провайдеров, без денег и без принятых развилок.

| # | Батч | CoD | Размер | WSJF | Примечание |
| --- | --- | --- | --- | --- | --- |
| B41 | Ценовые зонды: проверка гипотез цен | 35 | 2 | **17.5** | владелец, не агент |
| B3a | Гайд валидации Phase 0: скрипт интервью и пороги | 23 | 2 | **11.5** | |
| B55 | Чек-лист go-live и брифинг юристу | 29 | 2 | **14.5** | разблокирует юриста |
| B56 | Правовая позиция по чужим фонограммам | 43 | 3 | **14.33** | экзистенциальный риск |
| B60 | Спека гарантий удаления и сроков хранения | 38 | 3 | **12.67** | вход для дизайн-дока хранилища |
| B57 | Карта данных, 152-ФЗ, локализация, оператор | 35 | 3 | **11.67** | |
| B2 | Стенд: статическая выкладка и раздел в README | 26 | 3 | **8.7** | размер поднят с 2 |
| B40 | Ролевая модель и правила выдачи: дизайн-док | 23 | 3 | **7.67** | |
| B58 | Версионирование согласий | 23 | 3 | **7.67** | задним числом не чинится |
| B11a | Реестр юр-обещаний, модуль констант, тест | 31 | 2 | **15.5** | подготовка без юриста |
| B12a | Протокол бенчмарка, корпус, разметка, прогон Demucs | 23 | 5 | **4.6** | ключей и бюджета НЕ требует |
| B8 | Полнота слоя проверки: 5 статусов, свой комментарий | 12 | 3 | **4.0** | |
| B5 | Типизированная настройка вместо разбора по подписям | 11 | 3 | **3.67** | до B6 |
| B6 | Персистентность проектов между перезагрузками | 11 | 3 | **3.67** | после B5 |
| B42 | Активация и воронка: определение и словарь событий | 15 | 3 | **5.0** | |
| B7 | Плюрализация счетчиков | 7 | 2 | **3.5** | после B4 и B8 |
| B4b | Звук: волна, плеер, клик | — | 5 | — | |
| B43 | Онбординг и первое ценностное действие | 16 | 5 | **3.2** | см. поправку ниже |
| B9 | Дизайн-док аналитики (без подключения) | — | 2 | — | размер снижен с 5 |
| B14a | Проверка мобильной раскладки на устройстве, отчет | — | 2 | — | нужен девайс |
| B13 | Статус `.claude/skills`: пометить vendored | 5 | 1 | **5.0** | вариант D |
| B3b | Провести интервью и заполнить лог | — | — | — | владелец; без этого Phase 0 не закрыта |
| B11b | Вставка утвержденных юр-текстов | — | 1 | — | заблокирован юристом |
| B14b | Правки по итогам мобильной проверки | — | — | — | размер после отчета |

## Phase 1: backend, хранилище, аудио

Порядок сборки отличается от порядка по WSJF, и это обосновано.

**B10 → B16 → B17 → B22 → B19a → B18 → B19b → B20 → B21.**

`B10` идет до backend: схема не зависит от выбора стека, если писать ее как
JSON Schema, а не как TypeScript-интерфейсы. `B22` поднят выше сепарации — он
замыкает сквозной путь на дешевых артефактах до вложений в провайдера.
`B19a` (пульс, сетка, размер, клик) поднят выше `B18`, потому что считается
по миксу и не зависит от сепарации, а `B18` — самый заблокированный батч пачки.

| # | Батч | CoD | Размер | WSJF |
| --- | --- | --- | --- | --- |
| B15 | Выбор backend-стека + ADR | 27 | 1 | **27.0** |
| B10 | SongGraph как реальная модель | 19 | 8 | **2.38** |
| B16 | Backend-скелет и загрузка в объектное хранилище | 21 | 13 | **1.62** |
| B17 | Очередь задач, прогресс, retry | 16 | 8 | **2.0** |
| B22 | Реальный ZIP и скачивание | 9 | 5 | **1.8** |
| B19 | Анализ BPM, тональности, размера, структуры, аккордов | 19 | 13 | **1.46** |
| B18 | Stems: адаптер и один драйвер | 31 | 13 | — |
| B20 | Транскрипция audio в MIDI | 18 | 13 | **1.38** |
| B21 | Нотация MusicXML в PDF | 20 | 20 | **1.0** |
| B61 | Безопасность приема файла | 31 | 5 | **6.2** |
| B63 | Аудит-лог и управление секретами | 19 | 3 | **6.33** |
| B67 | Бэкапы и проверенное восстановление | 20 | 3 | **6.67** |
| B64 | Метрики качества ML и стоимости обработки | 18 | 3 | **6.0** |
| B66 | Окружения, релиз, миграции | 15 | 5 | **3.0** |
| B12b | Прогоны по платным API и сводная таблица | — | 3 | — |
| B65 | Регресс качества моделей на эталоне | 14 | 5 | **2.8** |

**Дизайн-доки интеграций** (пишутся прямо перед сборкой, не пакетом):
`INTEGRATION_STORAGE_DESIGN` перед B16, `INTEGRATION_STEMS_DESIGN` перед B18,
`INTEGRATION_TRANSCRIPTION_DESIGN` перед B20 если Klangio.
Спеки без внешней интеграции: `SONGGRAPH_SPEC`, `PROCESSING_LIFECYCLE_SPEC`,
`EXPORT_BUNDLE_SPEC`, `ANALYSIS_SPEC`, `TRANSCRIPTION_SPEC`, `NOTATION_SPEC`.

## Груминг функциональности и UX/UI

Две пачки, 50 находок с адресами в коде: `vokal-product-agent` по функциональности
и `vokal-design-agent` по интерфейсу. Пересечений много, поэтому сведены вместе.
Закрытое отмечено; остальное — очередь.

**Закрыто в B70** (все всплыли бы в первые минуты интервью):

- Коммерческий трек объявлялся низкокачественным и ронял обработку: правило
  считало клиппинг признаком непригодности, а современный мастеринг штатно
  упирается в потолок. Техническая пригодность (моно, низкая частота) разведена
  с громкостью.
- «Посмотреть на демо-разборе» возвращало на первый экран вместо демо.
- Свой файл назывался «демо-разбором» на кнопке, в заголовке обработки и в шаге
  уточнений.
- Три вкладки показывали пустоту без объяснения; «0 в работе» на «Проверке»
  читалось как «проверено, проблем нет» — обратное правде.
- Командная полоса показывала «0/0», «0», «0/0» вместо состояния проекта.
- Отсутствие разбора было покрашено стилем успеха, а сводка зрелости утверждала,
  что форма и аккорды собраны.

**Очередь, по пользе на единицу размера:**

| # | Находка | Размер | До интервью |
| --- | --- | --- | --- |
| B71 | Восемь шагов обработки рапортуют об успехе, результата нет | 5 | да |
| B72 | Восемь материалов показывают одинаковое превью; `preview.lines` не выводится нигде | 3 | да |
| B73 | Solo и Mute без обработчиков; транспорт без позиции | 2 | да |
| B74 | Откат работает только к непосредственно предыдущей версии; у «Оригинала» снимка нет никогда | 5 | да |
| B75 | Команда пользователя не сохраняется; нераспознанное выполняется как «усилить припев» | 2 | да |
| B76 | Клик по логотипу уничтожает сеанс без подтверждения и без возврата в проект | 3 | да |
| B77 | Возврат из демо перетирает выбранную задачу, подсветка карточки начинает врать | 3 | да |
| B78 | Уточнения: подписи расходятся, отредактированное поле не доезжает, доступ заперт за файлом | 3 | |
| B79 | Цель обработки ни на что не влияет и исчезает из результата | 3 | |
| B80 | Откат воскрешает удаленные результаты; `pending` обслуживает две разные причины | 3 | |
| B81 | Новая версия не устаревает выданные ссылки | 3 | |
| B82 | Версии: нельзя принять, нельзя различить, легко наплодить | 3 | |
| B83 | Выдача неготового материала не блокируется | 3 | |
| B84 | Семантика вкладок: `aria-controls` в никуда, нет стрелок, фокус не следует | 3 | |
| B85 | Загрузка: фокус невидим, перетаскивание не обработано, гонка при втором файле | 3 | |
| B86 | Первый экран переполняется на 1024 px; служебные полосы съедают планшет | 3 | на устройстве |
| B87 | На телефоне выбор материала не приводит к материалу | 3 | на устройстве |
| B88 | Состояние передается только цветом; прогресс без роли и значения | 3 | |
| B89 | Учебный контур: класс, ансамбль, задания и роли выдачи не выведены на экран | 5 | для школы |
| B90 | Учебное демо противоречит себе: индивидуальный урок с тремя чужими учениками | 2 | для школы |
| B91 | Ось зрелости объясняется четырежды разными словами | 2 | |
| B92 | Сценарий не назван на первом экране, карточки не сгруппированы | 3 | |
| B93 | Мелкие огрехи разметки: `100vh` против `100dvh`, `scroll-behavior` без `prefers-reduced-motion` | 2 | |

**Отдельно.** B78, B72 и B80 пересекаются с уже записанными долгами B5, B8 и B4:
подписи, снимок настроек и оценка сложности живут в одном узле кода, и разносить
их по разным батчам дороже, чем закрыть одним.

## Phase 2-3: ядро аранжировки и AI-копилот

Это собственное ядро продукта: то, что не покупается у провайдеров. Пакет
заблокирован Phase 1 и развилкой 1, поэтому срочность по нему низкая осознанно —
высокие числа стоят только там, где решение дорого менять задним числом.

| # | Батч | CoD | Размер | WSJF |
| --- | --- | --- | --- | --- |
| B28 | Транспозиция | 26 | 5 | **5.2** |
| B25 | Модель инструмента и исполнителя | 21 | 5 | **4.2** |
| B31 | Метрика сложности партии | 21 | 5 | **4.2** |
| B26 | DirectorAction DSL и rules engine | 33 | 8 | **4.13** |
| B23 | SongGraph как операционная модель | 28 | 8 | **3.5** |
| B24 | Confidence and Review Engine | 26 | 8 | **3.25** |
| B36 | Регенерация затронутых артефактов | 26 | 8 | **3.25** |
| B38 | LLM-адаптер AI-директора | 16 | 5 | **3.2** |
| B27 | Version and Diff Engine | 24 | 8 | **3.0** |
| B34 | Pedagogy Engine | 24 | 8 | **3.0** |
| B30 | Перенос партии на другой инструмент | 16 | 8 | **2.0** |
| B37 | Аудио-превью результата правки | 15 | 8 | **1.88** |
| B29 | Слияние партий: две гитары в одну | 24 | 13 | **1.85** |
| B32 | Пересборка партии под целевой уровень | 24 | 13 | **1.85** |
| B39 | Команды на естественном языке, карточки, отмена | 21 | 13 | **1.6** |
| B33 | Секционная драматургия: усиление и концовка | 15 | 13 | **1.15** |
| B35 | Ансамблевая раскладка | 15 | 13 | **1.15** |

**Порядок сборки ведет не WSJF, а цепочка «результат нечем проверить»:**

**B23 → B24 → B25 → B26 → B27** (плита) → **B28 → B36 → B37** (первая полная
петля на простейшей операции) → **B31 → B32** → **B29 → B30** → **B34** →
**B38 → B39** → **B33 → B35**.

Уверенность ставится сразу после графа, а не потом: это свойства узлов, и
дошивать их задним числом дороже. DSL ставится до любой операции — иначе первая
же операция создаст обходной путь мутации, и граница «LLM не правит музыку»
будет потеряна навсегда. Копилот идет последним из основного: LLM тем полезнее,
чем больше настоящих действий может выбрать; обратный порядок дает демонстрацию,
за которой ничего нет.

Если сегментом V1 станет музыкальная школа (развилка 5), B34 и B35 поднимаются
выше B29 и B30. Это единственная развилка, переставляющая порядок внутри пакета.

### Граница детерминированного слоя и LLM

Это граница безопасности продукта, а не архитектурное предпочтение.

**LLM можно:** объяснять структуру и проблемы, ранжировать и предлагать действия
из реестра, формулировать объяснения и заметки, задавать уточняющие вопросы,
переводить фразу пользователя в намерение.

**LLM нельзя ни при каких условиях:** писать в SongGraph (высоты, ритм,
длительности, аккорды, границы секций, темп, размер); создавать версии и
артефакты; выбирать аппликатуру, распределение по рукам и струнам, энгармонику;
назначать confidence и снимать пометки ревью; решать, исполнимо ли что-то;
расширять набор действий на лету; отвечать о содержании песни без опоры на граф;
участвовать в подсчете лимитов и в решениях о выдаче и удалении.

**Механика.** Единственный вход LLM — типизированное предложение «действие,
параметры, селектор, обоснование», проходящее четыре фильтра: схема,
существование адресов в графе, `check` DSL, подтверждение пользователем.
Проверяется архитектурным тестом: пути записи графа из LLM-адаптера не
существует. Тест, а не соглашение.

### Как проверять качество музыкального результата

Отдельная задача: код может не падать, а музыка быть мертвой.

1. **Золотой корпус.** 20-30 песен с ручной разметкой музыкантом: форма,
   тональность, аккорды, хуки, роли партий, уровни сложности. Тот же корпус
   обслуживает бенчмарк провайдеров (B12) — работа не двойная.
2. **Музыкальные инварианты в CI.** Каждая операция прогоняется по корпусу и
   падает как юнит-тест: гармония не изменилась, если операция этого не
   заявляла; форма не изменилась; ноль нарушений исполнимости; все ноты в
   диапазоне; размеченные хуки на месте; метрика сложности в целевом коридоре;
   обратимая операция обратима; Easy, Original-like и Advanced ложатся на одну
   сетку.
3. **Слепая экспертная оценка.** 3-5 музыкантов слушают A/B без подписей — наш
   результат, сырая транскрипция, ручная адаптация — по четырем вопросам:
   узнаваемо, исполнимо моим составом, готово к репетиции без правок, стал бы
   платить. Порог задается до прослушивания. Единственный способ поймать
   «формально корректно, музыкально мертво».
4. **Полевые прокси в проде.** Доля операций без последующих ручных правок; доля
   версий, дошедших до «принято для репетиции»; доля откатов. Обязаны улучшаться
   от батча к батчу, иначе движок улучшается только на бумаге.
5. **Регрессия.** Каждый найденный музыкальный дефект становится кейсом корпуса.

### Что защищает от копирования, а что обязательная гигиена

**Защита — накапливается и копируется тяжело:** SongGraph как операционная
модель с инвариантами (копируется идея, не копируется дисциплина); модель
ограничений инструмента и исполнителя (это данные, а не алгоритм); метрика
сложности с калибровкой (актив — размеченный корпус); правила слияния, переноса
и перестройки с явным списком потерь (рецепты аранжировщика, оформленные как
правила); педагогика и ансамблевая раскладка (доменное знание преподавателей);
связка «уверенность → допустимость автоматики → ревью»; золотой корпус и
инварианты.

**Гигиена — нужна, но не отличает:** версии и diff, регенерация артефактов,
аудио-превью, LLM-адаптер, команды на естественном языке, транспозиция.

Копируется интерфейс копилота. Не копируется дисциплина «каждое изменение —
проверяемая музыкальная операция с известными потерями».

## Phase 4: команды и монетизация

**Phase 4 целиком сейчас не берется.** Из пятнадцати батчей только четыре
исполнимы на текущем состоянии; остальные стоят за развилкой 1, за B16 и,
главное, за отсутствием настоящих артефактов. Продавать пока нечего.

| # | Батч | CoD | Размер | WSJF |
| --- | --- | --- | --- | --- |
| B52 | Дизайн-док платежей РФ | 30 | 3 | **10.0** |
| B44 | Аутентификация и личный аккаунт | 29 | 8 | **3.63** |
| B51 | Квоты, лимиты, мягкая деградация | 18 | 5 | **3.6** |
| B50 | Учет потребления и модель стоимости | 26 | 8 | **3.25** |
| B53 | Первая оплата физлиц и возврат тарифной оси | 36 | 13 | **2.77** |
| B54 | Оплата школами по счету | 21 | 8 | **2.63** |
| B46 | Сохраненные составы и профили музыкантов | 13 | 5 | **2.6** |
| B49 | Реальная выдача: ссылки, права, отзыв, устаревание | 31 | 13 | **2.38** |
| B47 | Библиотека песен и пресеты аранжировок | 13 | 8 | **1.63** |
| B45 | Пространства, роли, приглашения | 21 | 13 | **1.62** |
| B48 | Школьный контур: классы, ансамбли, назначения | 21 | 13 | **1.62** |
| B59 | Школьный контур и несовершеннолетние: ограничения | 23 | 5 | **4.6** |
| B62 | Роли, приватность по умолчанию, правила выдачи | 26 | 5 | **5.2** |
| B69 | Поддержка, жалобы, возвраты, takedown | 13 | 3 | **4.33** |
| B68 | Реакция на инцидент и утечку, статус-страница | 13 | 3 | **4.33** |

**Роли для первой платной версии — только две.** Владелец аккаунта и плательщик;
получатель материалов по ссылке без аккаунта. Платит один, играют пятеро —
требовать регистрации от каждого участника значит потерять выдачу как канал.
Все остальные роли откладываются. Как только появляется ученик с личным
аккаунтом, включается блок родительских согласий и обработки данных
несовершеннолетних; модель «преподаватель платит и владеет, ученик открывает
ссылку» позволяет отложить этот блок целиком.

## Чек-лист go-live

Блокирующее — без этого доступ снаружи не открываем.

1. Ответы юриста получены письменно; правовая позиция по фонограммам в ADR.
2. Оферта, политика ПДн и тексты согласий опубликованы и доступны из продукта.
3. Согласия версионированы; обработка без действующего согласия не стартует.
4. Роль оператора определена, локализация выполнена, уведомление подано если требуется.
5. С каждым провайдером есть договор об условиях хранения, удаления и неиспользования материалов для обучения моделей.
6. Удаление реально работает и проверено по всем каналам: версии объектов, логи, кеш, копии у провайдера, отзыв выданных ссылок.
7. Выдача идет через эндпоинт с проверкой прав; отзыв по уже выданной ссылке работает.
8. Приватность по умолчанию: нет публичного списка, нет индексации, ролевой доступ работает.
9. Проверка формата по содержимому, лимиты по длительности, изоляция декодера, ограничение частоты.
10. Аудит-лог пишется; доступ поддержки к чужому проекту фиксируется.
11. Секретов в репозитории и в истории нет, сторож в CI работает.
12. Бэкапы делаются, восстановление фактически проверено с фиксацией результата.
13. Runbook инцидента существует, рубильник работает, шаблоны уведомлений готовы.
14. Канал поддержки и канал для правообладателя работают, политика возвратов опубликована.
15. Минимальный набор наблюдаемости и алертов включен.
16. Прод отделен от тестового контура; продовые данные в тестовый контур не копируются.
17. Тексты интерфейса не обещают того, чего продукт не исполняет; сторож честности расширен под новые обещания.
18. Школьный контур: данных об учениках минимум, согласие за несовершеннолетнего оформлено.

Желательное: статус-страница, регресс качества моделей, дашборд себестоимости,
автовозврат кредитов при неуспешной задаче, заявленное время ответа поддержки,
подтверждение вторым человеком на массовое удаление, отчет о доступах для школы,
третье окружение.

## Что нельзя отложить технически

Здесь поздняя правка стоит непропорционально дорого или невозможна вообще.

1. **Версия согласия.** Если не сохранена в момент принятия — не существует. Единственный пункт, где данные не «дорого мигрировать», а «отсутствуют».
2. **Версионирование объектов в бакете.** Включенное по умолчанию превращает `DELETE` в пометку, и обещание удаления становится ложным задним числом.
3. **Механизм выдачи.** Подписанную ссылку нельзя отозвать. Если выдача идет голым signed URL, кнопка «удалить результаты» его не закрывает. Переход на отзываемую схему после запуска ломает все выданные ссылки.
4. **Политика логирования.** Ключи объектов и подписи в access-логах не переписываются; старые логи можно только удалить целиком.
5. **Аудит-лог.** «Кто открывал файл» задним числом не восстанавливается — значит объем утечки не установить и уведомить пострадавших нельзя.
6. **Связь артефакт — задача — версия модели.** Без нее нельзя ни объяснить регресс качества, ни отозвать результаты плохой версии.
7. **Схема событий без ПДн.** Очистка накопленного хранилища дороже правильной схемы сразу; переданное внешнему сервису не отзывается.
8. **Договорные условия с провайдерами.** Применяются к первому же отправленному файлу.
9. **Секреты в истории git.** Необратимо; лечится только ротацией.
10. **Происхождение эталонного набора.** Набор из чужих фонограмм нельзя ни хранить, ни ссылаться на его результаты — пересобирать придется вместе со всей разметкой.
11. **Обещания в интерфейсе.** Отзывать данное обещание дороже, чем не давать. «Удалить исходник» и «удалить результаты» уже обещаны.

## Вопросы юристу

Сформулированы так, чтобы допускать ответ «да» или «нет». Полный список из 23 —
в брифинге B55. Ключевые:

**Фонограммы и произведения.** Вправе ли мы обрабатывать охраняемую фонограмму,
если результат доступен только загрузившему и указанным им лицам? Достаточно ли
заверения пользователя, чтобы перенести на него риск? Является ли выдача stems
и минуса воспроизведением фонограммы — в отличие от нот и MIDI? Считается ли
автоматическая нотация переработкой произведения? Обязаны ли мы иметь публичный
канал приема заявлений правообладателей?

Здесь важно различение, которого не было в исходных документах: производные от
**фонограммы** (stems, минус, репетиционные треки) и от **произведения**
(ноты, MIDI, аккорды, текст) имеют разную правовую природу и разных
правообладателей. Продукт сегодня обещает их одним пакетом; ответ может
разрешить одно и запретить другое, и это меняет состав `Stage Pack`, а не экран.

**ПДн и 152-ФЗ.** Являемся ли мы оператором, обязанным подать уведомление?
Обязаны ли обеспечить запись и обновление базы на территории РФ? Является ли
передача файла зарубежному провайдеру трансграничной передачей? Считается ли
голос в записи биометрическими данными? Достаточно ли договора поручения со
школой? Требуется ли согласие родителя для ученика младше 18?

**Хранение и удаление.** Вправе ли хранить исходник весь срок жизни проекта?
Вправе ли сохранять производные после удаления исходника? Считается ли
обязательство исполненным, если объект удален из рабочего хранилища, но остается
в резервной копии?

## Что взять первыми деньгами

Технический минимум: настоящий результат по чужой песне; вход и владение;
настоящее хранение с настоящим удалением; работающее скачивание пакета; учет
себестоимости и лимиты; платежный контур с чеком и возвратом; описанное
поведение при сбое оплаченной задачи.

Правовой и этический минимум: ответ юриста по фонограммам (гейт, а не
пожелание); продавец, оферта, политика возвратов и ПДн, версионированные
согласия; честная формулировка предмета — продаем экономию времени и подготовку
к репетиции, а не гарантированно точную партитуру; канал поддержки.

Тарифная ось возвращается в интерфейс **только вместе с работающей оплатой**, и
каждая цифра на экране обязана совпадать с той, что спишется. Сторож честности
при этом не отключается, а сужается: цены разрешены только на страницах тарифов
и оплаты и только при совпадении с единственным источником.

Отдельная этическая граница: не продавать подписку, пока не подтверждена
частота использования. Продать месячный доступ тому, кто берет песню в квартал,
законно и продуктово разрушительно — в сегменте, где все друг друга знают.

## Долги по критериям приемки спеки

Закрыто реально 13 из 18. Закрыто формально:

- **Настройка задачи.** Разбирается сопоставлением по русским подписям
  интерфейса: переименование подписи в макете молча вернет состав к
  демо-умолчаниям, и это не поймает ни typecheck, ни тест. → B5
- **Сомнительные такты.** В модели пять статусов, кнопок три: поставить
  «сомнительно» или вернуть «нужно проверить» нельзя. → B8
- **Оценка сложности.** Спека требует предварительную оценку на экране загрузки,
  до запуска. Сейчас она на экране обработки и в экспорте — пользователь узнает
  сложность после того, как согласился. Плюс после B4a стала видна вторая
  сторона: сложность считается подстрочным поиском по тексту настроек
  («сцен» → высокая), поэтому на трехсекундном файле показывается «высокая
  сложность, 10 единиц». Настоящая длительность теперь известна и должна
  участвовать в расчете. → B5, B4
- **Desktop и mobile.** Брейкпоинты есть, но jsdom раскладку не проверяет, а на
  реальном устройстве прототип не открывали. → B14a

**Phase 0 не закрыта продуктово.** Success-критерии роадмапа никем не проверены.
→ B3a, B3b, B2

## Поправки груминга к фактам

Груминг-агенты ошиблись в нескольких проверяемых утверждениях. Зафиксировано,
чтобы не тиражировалось:

- **`CLAUDE.md` не содержит `TODO` и раздела «Что заполнить»** с коммита `78c4bb1`.
  Четыре агента из шести утверждали обратное, один из них предлагал «починить это
  до старта B23». Читали устаревшее состояние; на выводы груминга это не влияет,
  но принимать фактические утверждения агентов на веру нельзя.
- **Первый экран показывает 6 карточек задач, а не 16 целей обработки.** 16 целей
  есть в домене, наружу выведены шесть; демо-путь — один клик. Обоснование B43
  завышало проблему втрое. Батч остается, премиса исправлена.
- **Сторож честности не обходит вкладку «Экспорт»** — она в цикле. Промах был
  в паттернах, а не в покрытии.

Отдельная история — четыре отказа сторожа честности подряд, каждый по новой
причине: литеральный список пропустил «3-5 минут»; проверка только
`document.body` не видела `<head>`; `\b` на кириллице не мог совпасть в
принципе; и наконец все сценарии шли через демо-путь, поэтому путь загрузки
с его «моковой подготовкой» не проверялся вовсе. Вывод для следующих сторожей:
мало правильного паттерна и правильной поверхности — тест обязан проходить по
коду, где нарушение может жить.
- Верны и проверены: `base` не задан в `vite.config.ts` (сборка на подпути
  сломается); `LegalConsent` не хранит версию; текст согласия продублирован в
  четырех местах трех файлов; лимит 50 МБ отсекает WAV от 5 минут (посчитано:
  50.5 МБ при 44.1/16/стерео) — лимит выставлен по размеру вместо длительности;
  сканера секретов в CI нет, хотя `CLAUDE.md` запрещает коммитить ключи.

## Отсутствующие артефакты процесса

Пять агентов независимо отметили, что `CHANGELOG.md` и `HANDOFF.md` в репозитории
нет и состояние живет только в этом файле. `CLAUDE.md` их не требует — решение,
заводить ли их, за владельцем. Пока роль передачи состояния несет `docs/BACKLOG.md`.
